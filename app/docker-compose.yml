services:
  db:
    container_name: smartSOL
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER : smartsol
      POSTGRES_PASSWORD : smartpass
      POSTGRES_DB: smartdb
    ports:
      - "5434:5432"
    volumes: 
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: